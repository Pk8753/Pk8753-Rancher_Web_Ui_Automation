{
  "name": "rancher_web_ui_automations",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypresstest": "npx cypress run --browser chrome --env user=$user,passwd=$passwd",
    "testlogin": "npx cypress run --browser chrome --spec cypress/integration/specs/RancherLogin.spec.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^12.17.2"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "cypress-mochawesome-reporter": "^3.0.0"
  }
}
